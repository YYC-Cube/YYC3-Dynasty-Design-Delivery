---
file: 0904-YYC3-链路衔接-守卫深链.md
description: 路由守卫/深度链接/URL状态同步/跨标签页同步
author: YYC³ Dynasty Team <admin@0379.email>
version: v1.0.0
created: 2026-06-14
updated: 2026-06-14
status: published
tags: [routing],[guard],[deep-link],[cross-tab],[url-sync]
category: design
language: zh-CN
audience: developers,architects
complexity: advanced
---

> **_YanYuCloudCube_**
> **_YYC³ Dynasty · 链路衔接_**
> **_三省以治，六部以行_**

# 路由守卫与深度链接

## 一、路由守卫层级

```
路由守卫三层架构:

┌─────────────────────────────────────────────┐
│ L1: 全局前置守卫 (beforeEach)                        │
│   ├─ 会话有效性校验（玉玺授权令牌过期检查）           │
│   ├─ 首次访问 → 重定向 /welcome                     │
│   ├─ 会话锁定 → 重定向 /welcome?locked=true          │
│   └─ 记录路由访问日志（太史监候审计）                │
├─────────────────────────────────────────────┤
│ L2: 路由级权限守卫 (meta.permission)                 │
│   ├─ 'public'     → 直接放行（P1 启动页）            │
│   ├─ 'logged-in'  → 需已登朝（P2/P3/P4/P6/P7/P8）  │
│   ├─ 'emperor'    → 仅天子（P11 宫阙规制）           │
│   ├─ 'crown-prince' → 天子/太子（P5 旨意工坊创建）   │
│   └─ 'zhongshu/menxia/shangshu' → 对应省官员        │
├─────────────────────────────────────────────┤
│ L3: 资源级守卫 (beforeResolve)                       │
│   ├─ 校验路由参数格式（如 edict/:id 中 id 为数字）    │
│   ├─ 校验目标资源是否存在（404 判断）                 │
│   └─ 校验数据版本号（stale 数据重拉取）               │
└─────────────────────────────────────────────┘
```

## 二、深度链接（Deep Link）处理

| 场景 | URL 示例 | 行为 |
|------|---------|------|
| 直接访问敕令详情 | `/edict/042` | 鉴权→展开P9弹窗 |
| 直接访问特定朝代 | `/timeline?dynasty=唐` | P3自动选中"唐" |
| 锚点定位朝堂节点 | `/court?node=天堂` | P2自动聚焦"天堂" |
| 带筛选的旨意板 | `/edict?status=执行中&page=3` | P6预加载筛选 |
| 特定Family桥接 | `/bridge?family=元启·天枢` | P8高亮该Family |
| 会话锁定恢复 | `/welcome?locked=true&return=/edict` | 解锁后返回深链 |

## 三、URL 状态同步策略

| 数据类型 | 同步至 URL | 更新策略 | 页面 |
|---------|:---------:|---------|------|
| 当前筛选条件 | ✅ | `replaceState` 防抖 | P6 旨意板 |
| 分页页码 | ✅ | `pushState` 切换 | P6 旨意板 |
| 选中朝代 | ✅ | `replaceState` | P3 王朝 |
| 搜索关键词 | ✅ | `replaceState` 防抖 | 搜索面板 |
| 弹窗状态 | ❌ | — | — |
| 临时 hover | ❌ | — | — |
| 表单输入 | ❌ | — | — |

## 四、跨标签页数据同步

```
SharedWorker 方案:
  主标签页（朝堂）作为数据源，可读可写
  其他标签页作为只读副本

同步冲突解决:
  ├─ 版本号机制: 每个 Store 维护递增版本号
  ├─ 冲突检测: 写入时对比版本号，版本过期拒绝写入
  └─ 冲突提示: "敕令已被其他标签页更新，已自动刷新"
```

---

> **言启象限 | 语枢未来** — Words Initiate Quadrants, Language Serves as Core
